// Goal: When sentence has no linked data/view, suggest views that might support or illustrate it.

// Required Context:

// narrative_sentence

// view_catalog or index of available views

// data_schema (variables + values)

// Optional Context:

// semantic tags from views

// existing view-to-insight links